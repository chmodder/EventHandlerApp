using Windows.UI.Xaml.Controls;
using EventHandlerApp.View;

namespace EventHandlerApp.Common
{
    public class FrameNavigation
    {

        public static void LoadFrame(MainPage theMainPage, object pageName)
        {
            //Page thePageToNavigateTo = (Page)pageName;
            //Frame someFrame = (Frame)theMainPage.Frame.FindName("MainFrame");
            //someFrame.Navigate(typeof(pageName));
        }
    }
}